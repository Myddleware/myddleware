# /etc/cron.d/myddleware: crontab entries for Myddleware
# This file is in the system crontab directory format, which requires:
# - The username field between the time specification and the command
# - Proper line endings without user crontab expansion

PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
SHELL=/bin/bash

# Run Myddleware cron every 5 minutes
*/5 * * * * www-data /usr/local/bin/myddleware-cron.sh >> /var/log/myddleware-cron.log 2>&1

# Monitor running jobs every minute
* * * * * root /usr/local/bin/myddleware-monitor.sh >> /var/log/myddleware-monitor.log 2>&1
