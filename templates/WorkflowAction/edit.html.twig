{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit a Workflow Action</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.description) }}
        {{ form_row(form.Workflow) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}