{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} | {{ 'rulegroup.create'| trans}}{% endblock %}
{% block titlesm %}{{ 'rulegroup.create'| trans}}{% endblock titlesm %}
{% block body %}
    <div class="container mt-5">        
        {{ form_start(form) }}
        <div class="mb-3">
                <div class="row mb-4">
                    <div class="col-md-6">
                        {{ form_row(form.name) }}
                    </div>
                </div>
                <div class="row mb-4">
                    <div class="col-md-6">
                        {{ form_row(form.description) }}
                    </div>
                </div>
        </div>
        
        {{ form_end(form) }}
    </div>
{% endblock %}